# 关于 XSS 的总结和思考

分类

服务端和客户端

对于服务端的 XSS 攻击，防护手段就是对从服务端拿到的数据进行编码（可以使用库）。

客户端的 XSS 攻击，防护手段就是使用安全的 JS api

## Vue 框架中是如何进行 XSS 防护的

{{ }} 文本插值，v-bind 属性绑定会自动转义，默认安全。

### 注意

v-html 不会对内容进行转义，谨慎使用，如果必须要使用（如富文本展示），可以使用 DOMPurify
、sanitize-html 进行净化
